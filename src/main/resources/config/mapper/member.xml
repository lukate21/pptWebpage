<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="kr.co.ppt.dao.MemberDAO">
	
	<insert id="join" parameterType="member">
		insert into users(no, id, domain, password, name, tel, reg_date) 
		values(users_seq.nextval, #{id}, #{domain}, #{password}, #{name}, #{tel}, sysdate)
	</insert>
	
	<select id="login" parameterType="member" resultType="member">
		select id, domain, password
		form users
		where id=#{id} and domain=#{domain} and password=#{password}
	</select>
</mapper>